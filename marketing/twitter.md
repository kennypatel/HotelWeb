# Twitter/X Posts — Copy, Paste, Post

---

## Tweet 1 — Launch announcement

```
I got tired of copy-pasting Stripe boilerplate into every project.

So I built /stripe-launch — a Claude Code skill that does the entire integration in one command.

It generates:
→ Checkout endpoint (one-time or subscriptions)
→ Webhook handler with signature verification
→ Customer portal
→ React checkout button
→ Tests
→ Go-live guide

Works with Next.js, Django, Rails, Laravel, Go.

$99 one-time: [YOUR GUMROAD LINK]
```

---

## Tweet 2 — Problem/solution

```
Stripe integration checklist (every. single. time.):

□ Install SDK
□ Create checkout session endpoint
□ Handle success/cancel URLs  
□ Set up webhook endpoint
□ Figure out raw body parsing (always breaks)
□ Verify signature
□ Handle checkout.session.completed
□ Handle invoice.payment_failed
□ Customer portal
□ Write tests
□ Write go-live guide

OR: /stripe-launch in Claude Code

[YOUR GUMROAD LINK]
```

---

## Tweet 3 — Social proof / demo

```
Here's what /stripe-launch (my Claude Code skill) actually outputs on a Next.js project:

app/api/checkout/route.ts ← Stripe Checkout session
app/api/webhook/route.ts ← Webhook + signature verification  
app/api/portal/route.ts ← Customer billing portal
components/CheckoutButton.tsx ← Drop-in React component
__tests__/payments.test.ts ← Mocked Stripe tests
STRIPE_SETUP.md ← Your go-live checklist

One command. Everything committed. $99:
[YOUR GUMROAD LINK]
```

---

## Tweet 4 — Value angle

```
A freelancer would charge $300-800 to integrate Stripe properly.

/stripe-launch does it in 5 minutes for $99.

And you can use it on every project forever.

[YOUR GUMROAD LINK]
```

---

## Thread format (for maximum reach)

**Tweet 1:**
```
I just shipped a Claude Code skill that adds complete Stripe payments to any web app in one command.

Here's exactly what it does 🧵
```

**Tweet 2:**
```
The problem: Stripe integration takes 6-10 hours every time.

Checkout session, webhook handler (with raw body parsing that always breaks), signature verification, customer portal, tests...

Same boilerplate. Every. Project.
```

**Tweet 3:**
```
The solution: /stripe-launch

One command in Claude Code. It detects your framework and generates everything:

→ Checkout endpoint (one-time or subscriptions)
→ Webhook handler with proper signature verification
→ Customer portal for subscribers
→ React checkout button component
→ Tests with mocked Stripe calls
→ STRIPE_SETUP.md go-live guide
```

**Tweet 4:**
```
Works with:
✓ Next.js 13/14/15 (TypeScript or JS)
✓ React + Express
✓ Django
✓ Flask / FastAPI
✓ Ruby on Rails
✓ Laravel
✓ Go

Auto-detected. Idiomatic code for each framework.
```

**Tweet 5:**
```
$99 one-time. Unlimited projects. Lifetime updates.

[YOUR GUMROAD LINK]

(The skill file is on GitHub if you want to see exactly what it does before buying)
```
